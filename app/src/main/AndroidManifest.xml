<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="deadbird.ua.millionergame">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon"
        android:supportsRtl="true"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">

        <activity android:name=".activity.SplashScreen"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".activity.MainActivity"
            android:screenOrientation="portrait"/>

        <activity android:name=".activity.GameActivity"
            android:screenOrientation="portrait"/>

        <activity android:name=".activity.PlayerHelperActivity"
            android:theme="@style/AppDialog"
            android:screenOrientation="portrait"/>

        <activity android:name=".activity.WinOrLoseFragmentActivity"
            android:theme="@style/AppDialog"
            android:screenOrientation="portrait"/>
        <activity android:name=".activity.WinActivity"
            android:screenOrientation="portrait"/>

    </application>

</manifest>
